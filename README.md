`requests` Needed

`mineru_token` file needed

Register and create a mineru token on https://mineru.net

```bash
mkdir -p path/to/sliders/input
# copy your slides into input/
uv run python main.py path/to/sliders
# cup of coffee
tree path/to/sliders/output
```
